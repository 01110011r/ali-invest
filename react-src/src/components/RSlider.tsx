import {useState} from "react";

function RSlider(configuration) {
    //conf={
    //min:number,
    //max:number,
    //prev:number,
    //current:number,
    //next:number,
    //paginationView:booleant
    // const [conf, setConf] = useState(configuration)

    return (
        <div>

        </div>
    );
}

export default RSlider;